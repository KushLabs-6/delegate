{
  "name": "delegate-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install --include=dev && node -e \"require('fs').copyFileSync('prisma/schema.production.prisma','prisma/schema.prisma')\" && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma db push --accept-data-loss && node dist/index.js"
  }
}
