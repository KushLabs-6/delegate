{
  "name": "delegate-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && cp prisma/schema.production.prisma prisma/schema.prisma && npm install --include=dev && npx prisma generate && npx prisma db push --accept-data-loss && npm run build",
    "start": "cd backend && npm start"
  }
}
